{
	"name": "cfp-test1",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"format": "biome format --write . && dprint fmt  && textlint --fix \"**/*.md\""
	},
	"devDependencies": {
		"wrangler": "^4.18.0"
	}
}
